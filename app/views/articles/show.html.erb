	<% @page_title = "Evento di ".concat"#{@article.society}".concat"-".concat"#{@article.title}" %>

<p>
  <b>Society:</b>
  <%=h @article.society %>
</p>

<p>
  <b>Title:</b>
  <%=h @article.title %>
</p>

<p>
  <b>Date:</b>
  <%=h @article.date.strftime("%d %B %y") %>
</p>

<p>
  <b>Description:</b>
  <%= @article.description%>
</p>

<p>
  <b>Immagine:</b>
  <%= image_tag @article.photo.url(:thumb), :class=>"img_articolo" %>
  	<% if @article.pdf_file_name != nil%>
			<%= button_to "Scarica il pdf", { :action => "send_pdf", :id => @article.id} %>
		<% end%>
</p>

<%if (session[:user_id]!=nil and (@user.society==@article.society or  @user.admin=="si"))%>
<%= link_to 'Edit', edit_article_path	 %> |
<% end%>
<%= link_to 'Back', articles_path %>
